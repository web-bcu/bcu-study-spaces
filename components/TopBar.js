'use client'
import Button from "./Button";

export default function TopBar() {
    return (
        <div className="absolute top-4 right-10 z-10">
            <Button onClick={() => {}} btnClass="btn-primary text-white uppercase" title='Sign in'/>
        </div>
        
    )
}